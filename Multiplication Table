#Multiplication Table


def multiplication_table(number):
    if number != 0:
        for n in range(1,11):
            print("{0} * {1} = {2}".format(number,n,n*number))
    else: print("Anything Multiplies with Zero will gives Zero")
        
multiplication_table(0)
